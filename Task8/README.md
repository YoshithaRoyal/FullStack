This project demonstrates a simple Employee Management Module using Spring Core.
It showcases key concepts such as Inversion of Control (IoC) and Dependency Injection (DI) using annotations like @Component and @Autowired.

The application uses Spring’s BeanFactory/ApplicationContext to manage beans and stores employee data in memory.
